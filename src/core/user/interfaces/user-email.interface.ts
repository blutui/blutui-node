export interface UserEmail {
  id: string
  object: 'user_email'
  email: string
  verified: boolean
}

export interface UserEmailResponse {
  id: string
  object: 'user_email'
  email: string
  verified: boolean
}
